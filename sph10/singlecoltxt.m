

% Open the file to write
fid = fopen('RBLANK.txt', 'w');

% Loop through each element and write to the file
for k = 1:size(RBLANK, 3) % Loop through the third dimension
    for j = 1:size(RBLANK, 2) % Loop through the second dimension
        for i = 1:size(RBLANK, 1) % Loop through the first dimension
            fprintf(fid, '%f\n', RBLANK(i, j, k));  % Write each element on a new line
        end
    end
end

% Close the file
fclose(fid);

% Open the file to write
fid = fopen('DELTA.txt', 'w');

% Loop through each element and write to the file
for k = 1:size(RBLANK, 3) % Loop through the third dimension
    for j = 1:size(RBLANK, 2) % Loop through the second dimension
        for i = 1:size(RBLANK, 1) % Loop through the first dimension
            fprintf(fid, '%f\n', RBLANK(i, j, k));  % Write each element on a new line
        end
    end
end

% Close the file
fclose(fid);
% Open the file to write
fid = fopen('RBLANK.txt', 'w');

% Loop through each element and write to the file
for k = 1:size(RBLANK, 3) % Loop through the third dimension
    for j = 1:size(RBLANK, 2) % Loop through the second dimension
        for i = 1:size(RBLANK, 1) % Loop through the first dimension
            fprintf(fid, '%f\n', RBLANK(i, j, k));  % Write each element on a new line
        end
    end
end

% Close the file
fclose(fid);
% Open the file to write
fid = fopen('RBLANK.txt', 'w');

% Loop through each element and write to the file
for k = 1:size(RBLANK, 3) % Loop through the third dimension
    for j = 1:size(RBLANK, 2) % Loop through the second dimension
        for i = 1:size(RBLANK, 1) % Loop through the first dimension
            fprintf(fid, '%f\n', RBLANK(i, j, k));  % Write each element on a new line
        end
    end
end

% Close the file
fclose(fid);
% Open the file to write
fid = fopen('RBLANK.txt', 'w');

% Loop through each element and write to the file
for k = 1:size(RBLANK, 3) % Loop through the third dimension
    for j = 1:size(RBLANK, 2) % Loop through the second dimension
        for i = 1:size(RBLANK, 1) % Loop through the first dimension
            fprintf(fid, '%f\n', RBLANK(i, j, k));  % Write each element on a new line
        end
    end
end

% Close the file
fclose(fid);
% Open the file to write
fid = fopen('RBLANK.txt', 'w');

% Loop through each element and write to the file
for k = 1:size(RBLANK, 3) % Loop through the third dimension
    for j = 1:size(RBLANK, 2) % Loop through the second dimension
        for i = 1:size(RBLANK, 1) % Loop through the first dimension
            fprintf(fid, '%f\n', RBLANK(i, j, k));  % Write each element on a new line
        end
    end
end

% Close the file
fclose(fid);

